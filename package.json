{
  "name": "modelmeter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Agent-first measurement and instrumentation for AI models.",
  "scripts": {
    "backfill-openai": "node scripts/backfill-openai.mjs",
    "snapshot": "node scripts/snapshot.mjs",
    "check": "node scripts/validate.mjs && node scripts/audit.mjs",
    "validate": "node scripts/validate.mjs",
    "audit": "node scripts/audit.mjs"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "rss-parser": "^3.13.0"
  },
  "engines": {
    "node": ">=20"
  }
}
