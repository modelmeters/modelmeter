name = "modelmeter"
compatibility_date = "2026-06-01"
pages_build_output_dir = "./public"

[vars]
PRICING_DATA_URL = "https://raw.githubusercontent.com/modelmeters/modelmeter/main/pricing/current.json"

# Routes will be added once the Worker handler exists in functions/ or src/.
# V1 plan:
#   /estimate    -> Worker function (cost calculator)
#   /models      -> Worker function (list with current pricing)
#   /pricing.json-> Worker function or static passthrough to GitHub raw URL
#   everything else -> Pages static (public/)